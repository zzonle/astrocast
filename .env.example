# .env.example — Variables de entorno de ejemplo para desarrollo
# Copia este archivo a `.env` y ajusta los valores para tu entorno local.

# Seguridad
SECRET_KEY=your-secret-key-here

# Debug
DEBUG=True

# Base de datos — usa SQLite en dev, Postgres en producción.
# Ejemplo SQLite (local):
DATABASE_URL=sqlite:///db.sqlite3
# Ejemplo Postgres:
# DATABASE_URL=postgres://user:password@localhost:5432/astrocast_db

# Servicio externo de pronóstico (Node/NASA)
NODE_NASA_API_URL=https://nasa-private.vercel.app/api/probabilities/forecast

# Hosts permitidos (coma-separados)
ALLOWED_HOSTS=127.0.0.1,localhost
# Orígenes de confianza para CSRF (comas separados)
CSRF_TRUSTED_ORIGINS=http://127.0.0.1,http://localhost

# Opcionales
# Email backend (por defecto en settings está configurado a console para dev)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# (Si usas SMTP, define SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, etc.)

# Tiempo límite para la API externa (en segundos) — si lo quieres sobreescribir
# EXTERNAL_API_TIMEOUT=200
